# Optional: use PostgreSQL instead of SQLite (set DATABASE_URL in .env)
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: teamtask
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/data

volumes:
  pgdata:
